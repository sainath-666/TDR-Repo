# Minimal peer config override (Fabric 2.5 defaults)
peer:
  id: peer0.apcrda
  networkId: apcrda-tdr
  listenAddress: 0.0.0.0:7051
  chaincodeListenAddress: 0.0.0.0:7052
  address: peer0.apcrda:7051
  localMspId: APCRDAMSP
  keepalive:
    minInterval: 60s
    client:
      interval: 60s
      timeout: 20s
    deliveryClient:
      interval: 60s
      timeout: 20s
